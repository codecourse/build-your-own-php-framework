{% extends 'layouts/app.twig' %}

{% block content %}
    <h1>{{ name }}</h1>

    <div>
        {% for user in users %}
            <div><a href="/users/{{ user.id }}">{{ user.first_name }}</a></div>
        {% endfor %}
    </div>
{% endblock %}