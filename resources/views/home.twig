{% extends 'layouts/app.twig' %}

{% block content %}
    <h1>{{ name }}</h1>

    <div>
        {% for user in users %}
            <div>{{ user.name }}</div>
        {% endfor %}
    </div>
{% endblock %}